rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /sugorokuRooms/{roomId} {
      allow read, create, update: if request.auth != null
        && roomId.matches('^[0-9]{4}$');
      allow delete: if false;
    }
  }
}
